<style>
    .biz-page { background: #f6f7fb; min-height: 100vh; padding: 28px 20px 80px; }
    .section-card {
        background: #fff; border-radius: 16px; border: 1.5px solid #eef0f6;
        box-shadow: 0 2px 12px rgba(0,0,0,0.04); overflow: hidden;
    }
    .section-card .card-header {
        background: #fff; border-bottom: 1.5px solid #f1f4f9; padding: 18px 24px;
    }
    .table thead th {
        font-size: 0.72rem; font-weight: 700; letter-spacing: 0.07em;
        text-transform: uppercase; color: #94a3b8; background: #f8fafc;
    }
    .table tbody tr:hover { background: #f8fafc; }
    .table td { vertical-align: middle; font-size: 0.88rem; }
    .empty-state { padding: 32px; text-align: center; color: #94a3b8; font-size: 0.85rem; }
    .empty-state i { font-size: 2rem; display: block; margin-bottom: 8px; opacity: 0.4; }
    .avatar {
        width: 34px; height: 34px; border-radius: 50%;
        display: flex; align-items: center; justify-content: center;
        font-size: 0.8rem; font-weight: 700; flex-shrink: 0;
    }
    .btn-google-connect {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: #fff;
        border: 1px solid #dadce0;
        color: #3c4043;
        font-size: 0.8rem;
        font-weight: 500;
        padding: 6px 14px 6px 10px;
        border-radius: 999px;
        text-decoration: none;
        box-shadow: 0 1px 2px rgba(60,64,67,0.15);
        transition: box-shadow 0.15s, background 0.15s;
    }
    .btn-google-connect:hover {
        background: #f8f9fa;
        color: #202124;
        box-shadow: 0 1px 4px rgba(60,64,67,0.25);
    }
    .google-g-logo {
        width: 18px;
        height: 18px;
        flex-shrink: 0;
    }
    .badge-google-connected {
        background: #e8f5e9;
        color: #137333;
        border: 1px solid #ceead6;
        font-size: 0.72rem;
    }
    .btn-google-refresh {
        font-size: 0.7rem;
        border-color: #dadce0;
        color: #5f6368;
    }
    .btn-google-refresh:hover {
        background: #f1f3f4;
        border-color: #bdc1c6;
        color: #202124;
    }
</style>
